-----------------------------------------------------------8<-----------------------------------------------------------
 * Utiliser cometd ?
 * Utiliser akka micro-kernel ? 
 * Tester vagrant
-----------------------------------------------------------8<-----------------------------------------------------------
 + revoir le package transport: event(source, message)
 + check messages origin
 + générer le fichier XML des pièces automatiquement
 + créer une démo didactique
 ----------------------------------------------------------8<-----------------------------------------------------------
  
// TODO ? ne pas effectuer le zoom-in si le zoom est un zoom mort (avertir avec un none sound)
// TODO !? rendre optional le zoom